// handles two values/parameters
// (inherit from dual_values)..
// minimum range
// click left of 1st value = page down 1st value
// click right of 2ndt value = page up 2nd value
// alt --"-- pgup/dn both..
// click/drag range bar (between values) - drag both..
//      normal = stop when reaching end..
//      alt (or shift?) = decrease range.. (remember starting range & reset if we drag back)
// close to values/handles, drag one value..
// when reaching/touching other value:
//      stop    - normal    (can't drag further)
//      cross   - shift     (switch editing between the two values)
//      push    - alt       (push both values)
